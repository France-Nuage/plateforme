/**
 * Represent a zero trust network.
 */
export type ZeroTrustNetwork = {
  /**
   * The zero trust network id.
   */
  id: string;

  /**
   * The zero trust network name.
   */
  name: string;

  /**
   * The zero trust network type id.
   */
  zeroTrustNetworkTypeId: string;
};

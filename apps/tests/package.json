{
  "name": "@france-nuage/tests",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:control-plane": "playwright test --project=control-plane",
    "test:platform": "playwright test --project=platform --reporter=dot,html --trace=on",
    "test:worker": "playwright test --project=worker"
  },
  "devDependencies": {
    "@france-nuage/api": "*",
    "@faker-js/faker": "^9.5.1",
    "@france-nuage/types": "*",
    "@playwright/test": "^1.50.0"
  }
}

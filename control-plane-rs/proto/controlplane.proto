syntax = "proto3";
package controlplane;

service Instance {
    rpc Status (InstanceStatusRequest) returns (InstanceStatusResponse);
}

enum InstanceStatus {
    RUNNING = 0;
    STOPPED = 1;
}

message InstanceStatusRequest {
  string id = 1;
}

message InstanceStatusResponse {
  InstanceStatus status = 1;
}

/**
 * Represent a zero trust network type.
 */
export type ZeroTrustNetworkType = {
  /**
   * The zero trust network type id.
   */
  id: string;

  /**
   * The zero trust network type name.
   */
  name: string;
};

/**
 * @fileoverview RPC middleware exports for gRPC client configuration.
 *
 * This module provides centralized access to gRPC middleware functions used for
 * enhancing client-side RPC calls with cross-cutting concerns like authentication
 * and error handling.
 */

export * from './rpc';

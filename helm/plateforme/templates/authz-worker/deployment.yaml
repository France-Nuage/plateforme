{{- if .Values.authzWorker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "plateforme.fullname" . }}-authz-worker
  labels:
    {{- include "plateforme.labels" . | nindent 4 }}
    app.kubernetes.io/component: authz-worker
spec:
  replicas: {{ .Values.authzWorker.replicas }}
  selector:
    matchLabels:
      {{- include "plateforme.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: authz-worker
  template:
    metadata:
      labels:
        {{- include "plateforme.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: authz-worker
    spec:
      initContainers:
        {{- include "plateforme.waitForPostgres" . | nindent 8 }}
        {{- include "plateforme.waitForMigrations" . | nindent 8 }}
        {{- include "plateforme.waitForSpicedb" . | nindent 8 }}
        {{- include "plateforme.waitForSpicedbSchema" . | nindent 8 }}
      containers:
        - name: authz-worker
          image: {{ include "plateforme.imageWithOverrides" (dict "component" "authz-worker" "imageConfig" .Values.authzWorker.image "context" .) }}
          imagePullPolicy: {{ .Values.authzWorker.image.pullPolicy | default .Values.global.imagePullPolicy }}
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "plateforme.secretName" . }}
                  key: postgres-password
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "plateforme.secretName" . }}
                  key: database-url
            - name: SPICEDB_URL
              value: {{ include "plateforme.spicedbUrl" . | quote }}
            - name: SPICEDB_GRPC_PRESHARED_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "plateforme.secretName" . }}
                  key: spicedb-preshared-key
            {{- if .Values.authzWorker.config.logLevel }}
            - name: LOG_LEVEL
              value: {{ .Values.authzWorker.config.logLevel | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.authzWorker.resources | nindent 12 }}
{{- end }}

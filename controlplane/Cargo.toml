[workspace]

members = [
  "database",
  "derive_factory",
  "derive_repository",
  "hypervisors",
  "hypervisor_connector",
  "hypervisor_connector_proxmox",
  "hypervisor_connector_resolver",
  "infrastructure",
  "instances",
  "resources",
  "server",
  "synchronizer",
]

resolver = "3"

[profile.dev]
type-length-limit = 4194304 # 4MB (double the usual default)

[profile.release]
type-length-limit = 4194304
